#include <stdio.h>
/**
 * Main - Causes an infinite loop
 * Return: 0
 */

int main(void)
{
	int i:
		Printf("Infinite loop incoming: (\
n")
		i = 0;
	/*
	 * while(i < 10)
	 * {
	 * putchar (i);
	 * }
	 */
}
